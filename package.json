{
  "name": "purescript-hs-mapnik",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "rimraf": "^2.6.1"
  }
}
